---
name: shiguo-image
description: 石锅王图片制作专属技能。包含云南蒸汽石锅官方实物图素材库，支持调用 Nano Banana API 生成淘宝主图、小红书封面、抖音封面、图生图。当用户提到"石锅"、"石锅王"、"蒸汽石锅图片"、"石锅主图"、"加载石锅素材库"、"帮我做石锅图片"时激活此技能。
---

# 石锅王图片制作技能

## 素材库

- **技能内置图片**：`assets/`（6张官方实物图）
- **服务器主库**：`/root/.openclaw/workspace/assets/shiguo/`（持续更新，优先从这里读）
- **上下文文件**：`/root/.openclaw/workspace/assets/shiguo/SHIGUO_CONTEXT.md`

## 产品核心信息

**产品**：云南蒸汽石锅（品牌：石锅王）

核心卖点（图片/文案必须体现）：
- 🪨 云南天然石材手工制作
- 💧 **锅底中央只有一个圆形进气孔**（孔的尺寸较小；孔上方有一个小气阀）
- 🍲 蒸汽从底部进气孔进入，原汁原味，健康少油
- 🥘 **分两种规格**：单锅（单人）和鸳鸯锅（双格，两人用）

> ⚠️ **生图必读（三个关键特征，不能错）：**
> 1. 锅底圆孔**尺寸较小**（不能画得太大太夸张）
> 2. 圆孔**上方有一个更小的气阀**（小帽/阀门盖在圆孔上）
> 3. 分**单锅**和**鸳鸯锅**——鸳鸯锅内部有隔板分两格
> 生成时先按单锅生成，除非特别指定鸳鸯锅。

## 图片素材清单

| 文件 | 尺寸 | 推荐用途 |
|------|------|---------|
| shiguo_01_600x800.jpg | 600×800 | 详情页 |
| shiguo_02_700x700.jpg | 700×700 | **淘宝主图首选** |
| shiguo_03_1080x1440.jpg | 1080×1440 | **小红书/抖音首选（最高清）** |
| shiguo_04_700x700.jpg | 700×700 | 淘宝主图备选 |
| shiguo_05_960x1280.jpg | 960×1280 | 详情页/朋友圈 |
| shiguo_06_960x1280.jpg | 960×1280 | 详情页/朋友圈 |

## 进度反馈（每30秒）

生成图片时自动每30秒向飞书群发送进度消息：
- 开始：「🎨 开始生成：xxx，请稍候...」
- 进行中：「⏳ 正在生成 xxx，已用时 30 秒，请继续等待...」
- 完成：「✅ xxx 生成完成！用时 xx 秒。」

关闭进度反馈：加 `--no-notify` 参数

## ⚠️ 图片生成规则（必须遵守）

**只用 Nano Banana（AIsa Gemini）** — 不管什么情况，图片生成只走这一个。

- ❌ 禁用：智谱 CogView
- ❌ 禁用：DALL-E 3
- ❌ 禁用：flux / Stability AI / 任何其他图片 AI
- ✅ 只用：AIsa Gemini（api.aisa.one），Node.js 调用

如果 AIsa 出现 504/网关错误：**等待重试**，不要换其他 API。
重试策略：等 15 秒 → 重试，最多重试 5 次，仍失败则通知用户等待而不是换 API。

## Nano Banana / AIsa 配置

**API Key 设置**（二选一）：
```bash
# 方式1：环境变量
export NANO_BANANA_API_KEY=你的key

# 方式2：写入 .env 文件
echo "NANO_BANANA_API_KEY=你的key" >> /root/.openclaw/workspace/.env
```

**调用脚本**：`scripts/nano_banana_generate.py`

## 工作流程

### 1. 生成淘宝主图（图生图，默认方式）
```bash
# 用实物参考图 + Seedream 图生图（推荐）
python3 scripts/seedream_img2img.py \
  --ref assets/shiguo_02_700x700.jpg \
  --prompt "Professional e-commerce photo of Yunnan stone steam pot, ONE steam hole at bottom center, white background, 1920x1920" \
  --output output_taobao.jpg
```

### 2. 纯文生图（备用）
```bash
python3 scripts/seedream_img2img.py --text-only \
  --prompt "Yunnan stone steam pot, ONE hole at bottom center, white background, 1920x1920" \
  --output output_taobao.jpg
```

### 3. 生成小红书封面
```bash
python3 scripts/seedream_img2img.py \
  --ref assets/shiguo_03_1080x1440.jpg \
  --prompt "Yunnan stone steam pot lifestyle photo, warm tones, ONE steam hole at bottom, 1920x2560" \
  --output output_xhs.jpg
```

### 4. 查看素材库
```bash
python3 scripts/nano_banana_generate.py --mode list
```

### 5. 扩充图库（上传新素材）
用户在飞书发图片并说"存到石锅素材库"或"扩充图库"时：
```bash
python3 scripts/save_shiguo_image.py <message_id> <img_key1> [img_key2...] [--category 分类名]
```
图片自动编号保存，索引自动更新。支持分类：
- 默认：直接放 `/assets/shiguo/`
- 分类：`--category 产品细节` / `--category 使用场景` / `--category 包装`

## ⚠️ 图片生成工具规定

**只用 Nano Banana（即 AISA 的 `gemini-3-pro-image-preview`）**

- ✅ 唯一允许：Nano Banana（AISA key: `sk-u1fCN653hKQjeLNDtL2zH4srKxWzQbTWmIBMKOKpr0AGRkKg`）
- ❌ 禁止使用：智谱 CogView（质谱）
- ❌ 禁止使用：通义万相、文心一格、硅基流动等其他平台
- ❌ 禁止使用：DALL·E、Stable Diffusion 等非 Nano Banana 工具

如果 Nano Banana 暂时超时，**不要切换其他工具**，而是：
1. 告知用户"Nano Banana 当前响应慢，正在重试"
2. 每隔 30 秒发一次进度更新
3. 持续重试直到成功

## 制作注意事项

- 保持石锅真实外观，不能随意改变形状
- 突出锅底蒸汽孔这个核心特点
- 风格：自然、质朴、天然感
- 图生图 strength 建议 0.6-0.7（保留石锅原型）

## 参考文件

- 完整提示词模板：`references/prompt-templates.md`
- 图片生成脚本（含 Nano Banana）：`scripts/nano_banana_generate.py`
- 素材上传脚本：`scripts/save_shiguo_image.py`
